apiVersion: v1
kind: Service
metadata:
  name: sso-mocker
  labels:
    app: sso-mocker
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: sso-mocker
